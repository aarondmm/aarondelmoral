"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { ThemeToggle } from "@/components/theme-toggle";
export function SiteHeader() { const path = usePathname(); const english = path.startsWith("/en"); const prefix = english ? "/en" : ""; const languageUrl = english ? (path.replace(/^\/en/, "") || "/") : `/en${path === "/" ? "" : path}`; return <header className="fixed inset-x-0 top-0 z-50 border-b border-black/5 bg-[#f7f7f4]/85 backdrop-blur-lg dark:bg-[#101113]/85"><nav className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 md:px-8"><Link href={prefix || "/"} className="font-bold tracking-tight">AARON<span className="text-lime-600">.</span></Link><div className="flex items-center gap-3 text-sm font-medium md:gap-6"><a href={`${prefix}/#proyectos`}>{english ? "Projects" : "Proyectos"}</a><a href={`${prefix}/#sobre-mi`}>{english ? "About" : "Sobre mí"}</a><Link href={`${prefix}/blog`}>Blog</Link><Link href={languageUrl} className="font-bold text-lime-700">{english ? "ES" : "EN"}</Link><ThemeToggle/><a href={`${prefix}/#contacto`} className="rounded-full bg-zinc-950 px-3 py-1.5 text-white">{english ? "Let’s talk" : "Hablemos"}</a></div></nav></header>; }
