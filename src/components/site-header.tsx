"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { ThemeToggle } from "@/components/theme-toggle";
export function SiteHeader(){const path=usePathname();const en=path.startsWith("/en");const home=en?"/en":"/";const ids=en?{projects:"projects",about:"about",contact:"contact"}:{projects:"proyectos",about:"sobre-mi",contact:"contacto"};const anchor=(id:string)=>`${home}#${id}`;const blog=en?"/en/blog":"/blog";const lang=en?(path.replace(/^\/en/,"")||"/"):`/en${path==="/"?"":path}`;return <header className="fixed inset-x-0 top-0 z-50 border-b border-black/5 bg-[#f7f7f4]/85 backdrop-blur-lg dark:bg-[#101113]/85"><nav className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 md:px-8"><Link href={home} className="font-bold">AARON<span className="text-lime-600">.</span></Link><div className="flex items-center gap-3 text-sm font-medium md:gap-6"><a href={anchor(ids.projects)}>{en?"Projects":"Proyectos"}</a><a href={anchor(ids.about)}>{en?"About":"Sobre mí"}</a><Link href={blog}>Blog</Link><Link href={lang} className="font-bold text-lime-700">{en?"ES":"EN"}</Link><ThemeToggle/><a href={anchor(ids.contact)} className="rounded-full bg-zinc-950 px-3 py-1.5 text-white">{en?"Let’s talk":"Hablemos"}</a></div></nav></header>}
