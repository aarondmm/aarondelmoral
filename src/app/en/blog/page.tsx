import Link from "next/link";
import { SiteHeader } from "@/components/site-header";
import { getPublishedPosts } from "@/lib/public-content";
export default async function EnglishBlog() { const posts = await getPublishedPosts(); return <><SiteHeader/><main className="mx-auto min-h-screen max-w-6xl px-5 py-28 md:px-8"><Link href="/en" className="text-sm font-semibold text-lime-700">← Back home</Link><p className="mt-12 text-sm font-bold uppercase tracking-[.2em] text-lime-700">Journal</p><h1 className="mt-3 text-5xl font-bold tracking-tight md:text-7xl">Ideas in motion.</h1><p className="mt-5 max-w-xl text-lg text-zinc-600">Travel, skate and development: everything that inspires me on and off the screen.</p><div className="mt-12 grid gap-4 md:grid-cols-2">{posts.map((post) => <Link href={`/blog/${post.category}/${post.slug}`} key={post.id} className="rounded-3xl bg-white p-7 transition hover:-translate-y-1 hover:shadow-lg"><p className="text-xs font-bold uppercase tracking-widest text-lime-700">{post.category}</p><h2 className="mt-3 text-2xl font-bold">{post.title}</h2><p className="mt-2 text-zinc-600">{post.excerpt}</p></Link>)}</div></main></>; }
