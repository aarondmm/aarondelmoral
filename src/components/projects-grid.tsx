"use client";
import { AnimatePresence, motion } from "framer-motion";
import { useState } from "react";
import { projects } from "@/lib/content";
const filters = ["todos", "web", "app", "landing", "ecommerce"] as const;
export function ProjectsGrid() { const [filter, setFilter] = useState<(typeof filters)[number]>("todos"); const visible = projects.filter((p) => filter === "todos" || p.category === filter); return <><div className="mb-8 flex flex-wrap gap-2">{filters.map((item) => <button key={item} onClick={() => setFilter(item)} className={`rounded-full px-4 py-2 text-sm capitalize transition ${filter === item ? "bg-white text-zinc-950" : "bg-white/10 hover:bg-white/20"}`}>{item}</button>)}</div><motion.div layout className="grid gap-4 md:grid-cols-3"><AnimatePresence mode="popLayout">{visible.map((project, index) => <motion.article layout key={project.id} initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: .96 }} whileHover={{ y: -7 }} transition={{ duration: .25 }} className={`min-h-72 rounded-3xl p-6 text-zinc-950 ${index === 0 ? "bg-lime-300" : index === 1 ? "bg-orange-200" : "bg-sky-200"}`}><p className="text-sm uppercase tracking-widest text-black/55">{project.category}</p><div className="mt-24"><h3 className="text-2xl font-bold">{project.title}</h3><p className="mt-2 max-w-xs text-black/70">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.technologies.map((tech) => <span key={tech} className="rounded-full border border-black/15 px-2 py-1 text-xs">{tech}</span>)}</div></div></motion.article>)}</AnimatePresence></motion.div></>; }
