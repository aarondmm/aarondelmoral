"use client";
import { useState } from "react";
export function ContactForm() { const [status, setStatus] = useState<"idle" | "sending" | "sent" | "error">("idle"); const [feedback, setFeedback] = useState(""); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus("sending"); const form = new FormData(event.currentTarget); const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(form)) }); const body = await response.json(); if (response.ok) { setStatus("sent"); setFeedback("¡Gracias! Te responderé lo antes posible."); event.currentTarget.reset(); } else { setStatus("error"); setFeedback(body.error ?? "Ha ocurrido un error."); } } return <form onSubmit={submit} className="mt-8 grid gap-3 sm:grid-cols-2"><input required name="name" placeholder="Tu nombre" className="rounded-xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-lime-600"/><input required name="email" type="email" placeholder="tu@email.com" className="rounded-xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-lime-600"/><input name="company" tabIndex={-1} autoComplete="off" className="hidden"/><textarea required name="message" placeholder="Cuéntame qué tienes en mente" className="min-h-32 rounded-xl border border-black/10 bg-white px-4 py-3 outline-none focus:border-lime-600 sm:col-span-2"/><button disabled={status === "sending"} className="w-fit rounded-full bg-zinc-950 px-6 py-3 font-semibold text-white transition hover:scale-105 disabled:opacity-60" type="submit">{status === "sending" ? "Enviando..." : "Enviar mensaje"}</button>{status !== "idle" && <p className={`self-center text-sm ${status === "error" ? "text-red-700" : "text-zinc-700"}`}>{feedback}</p>}</form>; }
