"use client";
import { signInWithEmailAndPassword } from "firebase/auth";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { auth } from "@/lib/firebase/client";
export default function AdminLoginPage() { const router = useRouter(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); async function login(formData: FormData) { setLoading(true); setError(""); try { const user = await signInWithEmailAndPassword(auth, String(formData.get("email")), String(formData.get("password"))); const idToken = await user.user.getIdToken(); const response = await fetch("/api/auth/session", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ idToken }) }); if (!response.ok) throw new Error("Tu cuenta no tiene permisos de administrador."); router.push("/admin"); router.refresh(); } catch (cause) { setError(cause instanceof Error ? cause.message : "No ha sido posible iniciar sesión."); } finally { setLoading(false); } } return <main className="grid min-h-screen place-items-center px-5"><form action={login} className="w-full max-w-sm rounded-3xl bg-white p-8 shadow-xl"><p className="font-bold text-lime-700">AARON.</p><h1 className="mt-5 text-3xl font-bold">Panel privado</h1><p className="mt-2 text-sm text-zinc-600">Acceso exclusivo para administración.</p><div className="mt-7 grid gap-3"><input required name="email" type="email" placeholder="Email" className="rounded-xl border border-black/10 px-4 py-3"/><input required name="password" type="password" placeholder="Contraseña" className="rounded-xl border border-black/10 px-4 py-3"/><button disabled={loading} className="rounded-xl bg-zinc-950 px-4 py-3 font-semibold text-white disabled:opacity-50">{loading ? "Accediendo..." : "Iniciar sesión"}</button>{error && <p className="text-sm text-red-600">{error}</p>}</div></form></main>; }
